import { SelectFivePipe } from './select-five.pipe';

describe('SelectFivePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectFivePipe();
    expect(pipe).toBeTruthy();
  });
});
